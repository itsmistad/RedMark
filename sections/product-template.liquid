{% for tags_r in product.tags %}
  {% if tags_r contains 'upsell_' %}
    {% assign test = tags_r | remove: 'upsell_' %}
    {% assign test_1  = all_products[test] %}  
  {% endif %}
{% endfor %}

<div class="product-section__container page-width"  itemscope itemtype="http://schema.org/Product" id="ProductSection-{{ section.id }}" data-section-id="{{ section.id }}" data-section-type="product" data-enable-history-state="true"> 
  <meta itemprop="name" content="{{ product.title }}">
  <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
  <meta itemprop="image" content="{{ product.featured_image.src | img_url: '800x' }}">

  {%- assign current_variant = product.selected_or_first_available_variant -%}
  {%- assign product_thumb_size = '96x' -%}
  {%- assign product_image_zoom_size = '768x768' -%}
  {%- assign enable_zoom = section.settings.enable_zoom -%}

  {% case section.settings.image_size %}
  {% when 'small' %}
  {%- assign product_image_size_url = '345x' -%}
  {% when 'medium' %}
  {%- assign product_image_size_url = '530x' -%}
  {% when 'large' %}
  {%- assign product_image_size_url = '720x' -%}
  {% endcase %}
  
  {% comment %}
  {% if section.settings.show_share_buttons %}
    {% include 'social-sharing', share_title: product.title, share_permalink: product.url, share_image: product %}
  {% endif %}
  {% endcomment %}
  <div class="product-section">
    <div class="product-section__gallery-container">
      {%- assign featured_img_src = current_variant.featured_image.src | default: product.featured_image.src -%}
      {%- assign featured_img_alt = current_variant.featured_image.alt | default: product.featured_image.alt -%}
      {% include 'sale_tag' %}
      <div class="product-section__photo-wrapper">
        <div id="FeaturedImageZoom-{{ section.id }}" class="product-section__photo" {% if enable_zoom %}data-zoom="{{ featured_img_src | img_url: product_image_zoom_size }}"{% endif %}>
          <div class="zoomIn">
          <img itemprop="image" src="{{ product.featured_image.src | img_url: product_image_size_url }}" alt="{{ featured_img_alt }}" id="FeaturedImage-{{ section.id }}" class="{% if enable_zoom %}zoom_hover{% endif %} product-featured-img{% if enable_zoom %} js-zoom-enabled{% endif %}">
          </div>
        </div>
      </div>
      {% if product.images.size > 1 %}
        <div class="thumbnails-wrapper">
          <ul class="product-section__thumbnails product-section__thumbnails-{{ section.id }}">
            {% for image in product.images %}
              <li class="product-section__thumbnails-item">
                <a itemprop="url"
                  href="{{ image.src | img_url: product_image_size_url }}"
                  class="text-link product-section__thumbnail product-section__thumbnail--{{ section.id }}"
                  {% if enable_zoom %}data-zoom="{{ image.src | img_url: product_image_zoom_size }}"{% endif %}>
                  <img class="product-section__thumbnail-image" src="{{ image.src | img_url: product_thumb_size }}" itemprop="image" alt="{{ image.alt | escape }}">
                </a>
              </li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}
    </div>
    <div class="product-section__options-form">
      <h1 itemprop="headline" class="product-section__title">{{ product.title }}</h1>
      {% if section.settings.show_vendor %}
        <p itemprop="brand" class="product-section__vendor">{{ product.vendor }}</p>
      {% endif %}
      <div itemprop="offers" itemscope itemtype="http://schema.org/Offer">
        <meta itemprop="priceCurrency" content="{{ shop.currency }}">
        <link itemprop="availability" href="http://schema.org/{% if product.available %}InStock{% else %}OutOfStock{% endif %}">
        <p class="product-section__price product-section__price-{{ section.id }}{% unless current_variant.available %} product-price--sold-out{% endunless %}">
          {% if current_variant.compare_at_price > current_variant.price %}
            <s id="ComparePrice-{{ section.id }}">{{ current_variant.compare_at_price | money }}</s>
            <span class="product-price__price product-price__price-{{ section.id }} product-price__sale product-price__sale--single">
              <span id="ProductPrice-{{ section.id }}" itemprop="price" content="{{ current_variant.price | money_without_currency }}"> {{ current_variant.price | money }} </span>
            </span>
          {% else %}
            <s id="ComparePrice-{{ section.id }}" class="hide">{{ current_variant.compare_at_price | money }}</s>
            <span class="product-price__price product-price__price-{{ section.id }}">
              <span id="ProductPrice-{{ section.id }}" itemprop="price" content="{{ current_variant.price | money_without_currency }}"> {{ current_variant.price | money }} </span>
            </span>
          {% endif %}
        </p>
        <span class="stamped-product-reviews-badge stamped-main-badge" data-id="{{ product.id }}"></span>
        <form action="/cart/add" method="post" enctype="multipart/form-data" class="product-section__form" data-section="{{ section.id }}">
          {% unless product.options.size == 1 and product.variants[0].title == 'Default Title' %}
            {% for option in product.options_with_values %}
              <div class="selector-wrapper js product-section__form-item">
                <label {% if option.name == 'default' %}class="label--hidden" {% endif %}for="SingleOptionSelector-{{ forloop.index0 }}">
                  {{ option.name }}
                </label>
                <select class="single-option-selector single-option-selector-{{ section.id }} product-section__form-input" id="SingleOptionSelector-{{ forloop.index0 }}" data-index="option{{ forloop.index }}">
                  {% for value in option.values %}
                    <option value="{{ value | escape }}"{% if option.selected_value == value %} selected="selected"{% endif %}>{{ value }}</option>
                  {% endfor %}
                </select>
              </div>
            {% endfor %}
          {% endunless %}
          <select name="id" id="ProductSelect-{{ section.id }}" data-section="{{ section.id }}" class="product-section__form-variants no-js">
            {% for variant in product.variants %}
              {% if variant.available %}
                <option {% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %} value="{{ variant.id }}">
                  {{ variant.title }}
                </option>
              {% else %}
                <option disabled="disabled">{{ variant.title }} - {{ 'products.product.sold_out' | t }}</option>
              {% endif %}
            {% endfor %}
          </select>
          {% if section.settings.enable_swatches %}
            {% if product.available and product.variants.size > 1 %}
              {% for option in product.options %}
                {% include 'swatch' with option %}
              {% endfor %}
            {% endif %}
          {% endif %}
          {% if section.settings.show_quantity_selector %}
            <div class="product-section__form-item product-section__form-quantity-wrapper">
              <label for="Quantity" class="product-section__qty-label">{{ 'products.product.quantity' | t }}</label>
              <input type="button" value="-" class="product-section__qty-decrease">
              <input class="product-section__qty-input"
                type="number" name="quantity" id="Quantity" value="1" min="1" max="99" pattern="[0-9]*">
              <input type="button" value="+" class="product-section__qty-increase">
            </div>
          {% endif %}
          <div class="product-section__form-item product-section__form-item--submit">
            <button type="submit" name="add" id="AddToCart-{{ section.id }}" {% unless current_variant.available %}disabled="disabled"{% endunless %} class="btn product-section__form-cart-submit">
              <span id="AddToCartText-{{ section.id }}">
                {% unless current_variant.available %}
                  {{ settings.add_to_cart_button_soldouttext }}
                {% else %}
                  {{ settings.add_to_cart_button_text }}
                {% endunless %}
              </span>
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
  <div class="product-section__description-container">
    <div class="product-section__description-header">
      <div id="simple" class="product-section__header-item selected-header-item">
        The Simple
      </div>
      <div id="technical" class="product-section__header-item">
        The Technical
      </div>
    </div>
    <div id="description-simple" class="product-section__description" itemprop="description">
      {{ product.description }}
    </div>
    <div id="description-technical" class="product-section__description hidden"></div>
  </div>
  <div class="product-section__instagram-gallery text-center">
    {% if section.settings.show_empty_gallery_text == true %}
      <p class="product-section__empty-gallery-msg">
        <span class="upper">
          🤯<br/>
          Doh! We're sorry.
        </span><br/>
        We currently don't have any pictures of this product on our Instagram.<br/>
        <a href="{{ settings.instagram_link }}" target="_blank">Click here to check out our page!</a>
      </p>
    {% endif %}
  </div>
</div>
<div id="stamped-main-widget" class="stamped-main-widget" data-widget-style="standard"
  data-product-id="{{ product.id }}" data-name="{{ product.title | escape }}"
  data-url="{{ shop.url }}{{ product.url }}"
  data-image-url="{{ product.featured_image | product_img_url: "large" |replace: '?', '%3F' | replace: '&','%26'}}" 
  data-description="" data-product-sku="{{product.handle}}">
  {{ product.metafields.stamped.reviews }}
</div>

<div class="modal fade" id="upsell-modal" role="dialog">
  <div class="modal-dialog" data-expires="{{ settings.session_time }}" data-delay="1">
    <div class="modal-content">
      <div class="modal-header">
        <a href="/checkout">X</a>
        <h4 class="modal-title">Related Products</h4>
      </div>
      <div class="modal-body">
        {% for tags_r in product.tags %}
          {% if tags_r contains 'upsell_' %}
            {% assign test = tags_r | remove: 'upsell_' %}
            {% assign product_drop  = all_products[test] %} 
            <div class="upsell_products">
              <div class="popup_image">{{ upsell_second }}<img class="cart-section__image" src="{{ product_drop.featured_image.src | img_url: '200x200' }}" ></div>
              <div class="popup_title">{{ product_drop.title }}</div>
              <div class="popup_price">{{ product_drop.price | money }}</div>
              <form action="/cart/add" method="post" enctype="multipart/form-data" class="product-section__form product-section__form">
                {% unless product_drop.options.size == 1 and product_drop.variants[0].title == 'Default Title' %}
                  {% for option in product_drop.options_with_values %}
                    <div class="selector-wrapper js product-section__form-item">
                      <label {% if option.name == 'default' %}class="label--hidden" {% endif %}for="SingleOptionSelector-{{ forloop.index0 }}">
                        {{ option.name }}
                      </label>
                      <select class="single-option-selector single-option-selector-{{ section.id }} product-section__form-input" id="SingleOptionSelector-{{ forloop.index0 }}" data-index="option{{ forloop.index }}">
                        {% for value in option.values %}
                          <option value="{{ value | escape }}"{% if option.selected_value == value %} selected="selected"{% endif %}>{{ value }}</option>
                        {% endfor %}
                      </select>
                    </div>
                  {% endfor %}
                {% endunless %}
                <select name="id" id="ProductSelect" class="product-section__form-variants no-js">
                  {% for variant in product_drop.variants %}
                    {% if variant.available %}
                      <option {% if variant == product_drop.selected_or_first_available_variant %} selected="selected" {% endif %} value="{{ variant.id }}">
                        {{ variant.title }}
                      </option>
                    {% else %}
                      <option disabled="disabled">{{ variant.title }} - {{ 'products.product.sold_out' | t }}</option>
                    {% endif %}
                  {% endfor %}
                </select>
                <div class="product-section__form-item product-section__form-item--quantity">
                  <input type="number" id="Quantity" name="quantity" value="1" min="1" class="product-section__form-input" pattern="[0-9]*">
                </div>
                <div class="product-section__form-item product-section__form-item--submit custom_buton">
                  <button onclick="myFunction()" type="submit" name="add" id="AddToCart1" class="test btn product-section__form-cart-submit">
                    <span id="AddToCartText">
                      add to cart
                    </span>
                  </button>
                  {{ '//cdnjs.cloudflare.com/ajax/libs/font-awesome/4.0.3/css/font-awesome.css' | stylesheet_tag }}
                </div>
              </form>
            </div>
          {% endif %}
        {% endfor %}
      </div>
      <div class="modal-footer">
        <a href="/checkout"  > No, Thanks </a>
      </div>
    </div>
  </div>
</div>

{% comment %}
{{ collection.url }}
{{ 'products.product.back_to_collection' | t: title: collection.title }}
{% endcomment %}

{% unless product.empty? %}
  <script type="application/json" id="ProductJson-{{ section.id }}">
      {{ product | json }}
  </script>
{% endunless %}

<script>
  $(function() {
    /*
     * Add custom class to reviews stamp
     */
    var stampedIOBadge = $('.stamped-product-reviews-badge.stamped-main-badge');
    stampedIOBadge.on('DOMSubtreeModified', function() {
      var stampedBadgeSpan = stampedIOBadge.find('.stamped-badge');
      if (stampedBadgeSpan.length > 0 && !stampedBadgeSpan.hasClass('product-section__form-reviews-stamp'))
        stampedBadgeSpan.addClass('product-section__form-reviews-stamp');
     });

    /*
     * Populate descriptions and instagram gallery
     */
    var simpleButton = $('#simple');
    var technicalButton = $('#technical');
    var simpleDesc = $('#description-simple');
    var technicalDesc = $('#description-technical');
    if (simpleDesc.html().trim().length > 0) {
      var results = simpleDesc.find('.ig').length;
      for (let i = 1; i <= results; i++)
      {
        var igElement = simpleDesc.find(`.ig:first()`);
        if (igElement.length > 0)
          var postLink = igElement.text();
        var imgLink = (postLink.endsWith('/') ? postLink : postLink + '/') + 'media?size=m';
        igElement.remove();
        $('.product-section__empty-gallery-msg').hide();
        var gallery = $('.product-section__instagram-gallery');
        gallery.append(`<a href="${postLink}" target="_blank" class="product-section__ig-img"><img src="${imgLink}"/></a>`);
      }
      var newTechnical = simpleDesc.find('.technical');
      if (newTechnical.length > 0) {
        technicalDesc.html(newTechnical.html());
        newTechnical.remove();
      } else {
        // '.technical' wasn't found, so let's hide the whole The Techinical section
        technicalButton.css('display', 'none');
        technicalDesc.css('display', 'none');
      }
      var newSimple = simpleDesc.find('.simple');
      if (newSimple.length > 0) { // This one NEEDS to be last because at this point we've lost the other elements.
        simpleDesc.html(newSimple.html());
      } else if (newTechnical.length == 0) // '.simple' wasn't found, so change The Simple to "Description" if there's also no '.technical'
          simpleButton.text('Description');
      $('.stamped-main-widget').attr('data-description', simpleDesc.html().trim());
    } else {
      // No description, so hide ALL THE THINGS!
      $('.product-section__description-container').css('display', 'none');
    }
      

    /*
     * Description button functionality
     */
    $('.product-section__header-item').click(function() {
      var selectedClass = 'selected-header-item';
      if (!$(this).hasClass(selectedClass)) {
        var currentlySelected = $(`.product-section__header-item.${selectedClass}`);
        currentlySelected.removeClass(selectedClass);
        $(this).addClass(selectedClass);
        var currentDesc = $(`#description-${currentlySelected.attr('id')}`);
        var newDesc = $(`#description-${$(this).attr('id')}`);
        currentDesc.hide();
        newDesc.show();
      }
    })

    /*
     * Quantity inputs
     */
    $('.product-section__qty-input').focusout(function() {
      if ($(this).val() < 1)
      $(this).val(1);
      else if ($(this).val() > 99)
      $(this).val(99);
    });
    $('.product-section__qty-increase').on('click', function() {
      var oldVal = $(this).siblings('.product-section__qty-input').val();
      var newVal = parseInt(oldVal) + 1;
      if (newVal < 100)
        $(this).siblings('.product-section__qty-input').val(newVal);
    });
    $('.product-section__qty-decrease').on('click', function() {
      var oldVal = $(this).siblings('.product-section__qty-input').val();
      var newVal = parseInt(oldVal) - 1;
      if (newVal > 0)
        $(this).siblings('.product-section__qty-input').val(newVal);
    });

    /*
     * AddToCart input
     */
    var button = $('#AddToCart-product-template'),
    submitButt = $('#disable_001');
    button.on('click',function(e) {
      submitButt.prop('disabled',false); // NOT a toggle
    });

    /*
     * Enables image zoom functionality
     */
    var zoomImg = $('img.zoom_hover');
    zoomImg.zoomIn({});
  });
  $('#container_001').on( 'click', '#upsell_product', function(e) {
    $('#AddToCart-product-template').trigger('click');            
  });
</script>
<style>
  div#FeaturedImageZoom-product-template {
    overflow: hidden;
  }
  button#AddToCart:hover span {
    color: #fff;
  }
  .main {
    margin-top: 24px;
  }
  .template-product .modal-footer {
    padding-bottom: 14px;
  }
  button#AddToCart-product-template:hover span {
    color: #fff;
  }
  #popupDiv {
    display: none;
    color:#000 !important;
    margin: 8px 0px 2px 4px !important;
    padding: 5px 0px 8px 0px !important;
    -webkit-border-radius:  5px 5px 5px 5px;
    -moz-border-radius: 5px 5px 5px 5px !important ;
    border-radius: 5px 5px 5px 5px;
    background-color:rgba(100%,100%,100%,1) !important;
    border-width: 1px !important;
    border-color: rgba( 0, 20, 45, .5) !important;
    border-style: solid !important;
    color: rgba( 0%,0%,0%,1) !important;
    box-shadow: 2px 2px 2px rgba(0%,0%,0%,0.2) !important;
  }

  /*----------  Swatch Styles  ----------*/
  {% assign width = '50px' %}
  {% assign height = '35px' %}
  /* Label */
  .swatch .header {
    margin: 0.5em 0;
  }
  /* Hide radio buttons.*/
  .swatch input { 
    display:none;
  }
  .swatch label {
    border-radius:2px;
    float:left;
    min-width: {{ width }} !important; 
    height: {{ height }} !important;
    margin: 0;
    background-color: #000;
    font-size: 0.8125em;
    text-align: center;
    line-height: {{ height }};
    white-space: nowrap;
    text-transform: uppercase;
    font-weight: bold;
    color: #fff;
    transition: 0.3s;
  }
  .swatch label:hover {
    background-color: #555;
  }
  .swatch-element label { padding:0 10px; }
  .color.swatch-element label { padding:0; }
  /* Styling selected swatch */
  /* Slightly raised */
  .swatch input:checked + label {
    -webkit-box-shadow:0px 1px 2px rgba(0,0,0,0.8);
    -moz-box-shadow:0px 1px 2px rgba(0,0,0,0.8);
    box-shadow:0px 1px 2px rgba(0,0,0,0.8);
    transition: 0.2s;
    border-color:transparent;
  } 
  .swatch input:checked + label:hover {
    -webkit-box-shadow:0px 1px 6px 2px rgba(0,0,0,0.5);
    -moz-box-shadow:0px 1px 6px 2px rgba(0,0,0,0.5);
    box-shadow:0px 1px 6px 2px rgba(0,0,0,0.5);
    border-color:transparent;
  } 
  .swatch .swatch-element {
    float:left;
    -webkit-transform:translateZ(0); /* webkit flicker fix */
    -webkit-font-smoothing:antialiased; /* webkit text rendering fix */
    /* Spacing between buttons */
    margin:0px 10px 10px 0;
    /* To position the sold out graphic and tooltip */
    position:relative;
  }
  /* Image with the cross in it */
  .crossed-out { position:absolute; width:100%; height:100%; left:0; top:0; }
  .swatch .swatch-element .crossed-out { display:none; }
  .swatch .swatch-element.soldout .crossed-out { display:block; }
  .swatch .swatch-element.soldout label {
    filter: alpha(opacity=60); /* internet explorer */
    -khtml-opacity: 0.6;      /* khtml, old safari */
    -moz-opacity: 0.6;       /* mozilla, netscape */
    opacity: 0.6;           /* fx, safari, opera */
  }
  /* Tooltips */
  .swatch .tooltip {
    text-align:center;
    background:gray;
    color:#fff;
    bottom:100%;
    padding: 10px;
    display:block;
    position:absolute;
    width:100px;
    left:{{ width | remove: 'px' | to_number | divided_by: 2 | minus: 50 | plus: 2 }}px;
    margin-bottom:15px;
    /* Make it invisible by default */
    filter:alpha(opacity=0);
    -khtml-opacity: 0;
    -moz-opacity: 0;
    opacity:0;
    visibility:hidden;
    /* Animations */
    -webkit-transform: translateY(10px);
      -moz-transform: translateY(10px);
        -ms-transform: translateY(10px);
        -o-transform: translateY(10px);
            transform: translateY(10px);
    -webkit-transition: all .25s ease-out;
      -moz-transition: all .25s ease-out;
        -ms-transition: all .25s ease-out;
        -o-transition: all .25s ease-out;
            transition: all .25s ease-out;
    -webkit-box-shadow: 2px 2px 6px rgba(0, 0, 0, 0.28);
      -moz-box-shadow: 2px 2px 6px rgba(0, 0, 0, 0.28);
        -ms-box-shadow: 2px 2px 6px rgba(0, 0, 0, 0.28);
        -o-box-shadow: 2px 2px 6px rgba(0, 0, 0, 0.28);
            box-shadow: 2px 2px 6px rgba(0, 0, 0, 0.28);
    z-index: 10000;
    -moz-box-sizing:border-box; 
    -webkit-box-sizing:border-box; 
    box-sizing:border-box;
  }
  .swatch .tooltip:before {
    bottom:-20px;
    content:" ";
    display:block;
    height:20px;
    left:0;
    position:absolute;
    width:100%;
  }
  /* CSS triangle */
  .swatch .tooltip:after {
    border-left:solid transparent 10px;
    border-right:solid transparent 10px;
    border-top:solid gray 10px;
    bottom:-10px;
    content:" ";
    height:0;
    left:50%;
    margin-left:-13px;
    position:absolute;
    width:0;
  }
  .swatch .swatch-element:hover .tooltip {
    filter:alpha(opacity=100);
    -khtml-opacity:1;
    -moz-opacity:1;
    opacity:1;
    visibility:visible;
    -webkit-transform:translateY(0px);
      -moz-transform:translateY(0px);
        -ms-transform:translateY(0px);
        -o-transform:translateY(0px);
            transform:translateY(0px);
  }
  .swatch.error {
    background-color:#E8D2D2!important;
    color:#333!important;
    padding:1em;
    border-radius:5px;
  }
  .swatch.error p {
    margin:0.7em 0;
  }
  .swatch.error p:first-child {
    margin-top:0;
  }
  .swatch.error p:last-child {
    margin-bottom:0;
  }
  .swatch.error code {
    font-family:monospace;
  }
  .font_style {
    text-transform: uppercase;
    font-weight: 700;
    color: #788188;
    top: 6px;
  }
  .template-product .product-price__sale-label {
    top: 36px;
  }
  .template-product p.product-section__price.product-section__price {
    font-size:  {{ section.settings.product_price_text }};
  }
  @media screen and (max-width: 767px ) and (min-width: 320px) {
  .upsell_products form.product-section__form.product-section__form {
      width: 100%;
    }
  } 
  .template-product .modal-footer {
    border-top: 0px solid #e5e5e5;
  }
  .template-product .modal-header {
    text-align: center;
  }
  .template-product .modal-header a {
    position: absolute;
    right: 16px;
    border-radius: 50%;
    background-color: #ccc;
    width: 33px;
    padding: 5px;
  }  
  .template-product .upsell_products .popup_price {
    text-align: center;
  }  
  .template-product .upsell_products .popup_title {
    text-align: center;
  }
  @media screen and (max-width: 30000px) and (min-width: 1024px) {
    .upsell_products {
      width: 50%;
      float: left;
    }
  }
  @media (min-width: 768px) {
    .template-cart .modal-dialog {
      margin: 30px auto;
      width: 665px !important;
      top: 15% !important;
    }
  }
  @media screen and (max-width: 767px) and (min-width: 320px) {
    .template-cart .modal-dialog form.product-section__form {
      width: 100%;
    }
    .product-section__form-item.product-section__form-item--quantity label {
      font-size: 1.5625em;
    }
    .product-section__form-item.product-section__form-item--quantity input {
      font-size: 1.5625em;
    }
    .header {
      font-size: 1.5625em;
    }
    .text-center.return-link-wrapper {
      margin-top: 20px;
    }
    .no_default_upsell {
      width: 49% !important;
      float: none !important; 
      background-color: #7796a8;
      padding: 11px 0px;
      border-radius: 4px;
      pointer-events: none;
    }
    .template-product .upsell_products {
      padding-bottom: 45px;
    }
    .modal-footer {
      font-size: 0.875em;
    }
    .template-product .upsell_products .product-section__form-item.product-section__form-item--quantity input#Quantity {
      font-size: 1.125em;
      width: 100%;
    }
    .template-product .upsell_products .popup_title {
      font-size: 0.9375em;
    }
    .template-product .upsell_products .popup_price {
      font-size: 1.125em;
    } 
    .template-product .modal-header a {
      position: absolute;
      right: 31px;
      padding: 6px;
    }
  }  
  .template-product .btn-group-lg>.btn, .btn-lg {
    padding: 5px 16px;
    font-size: 1.125em;
    line-height: 1.3333333;
    border-radius: 6px;
  }
  input.no_upsell.btn.btn--small-wide {
    padding: 11px 25px;
    font-size: 1.125em;
    line-height: 1.3333333;
    border-radius: 2px;
  }
  .enable_pointer{
    pointer-events: all !important;
  }  
  .no_default_upsell {
    width: 31%;
    float: right;
    background-color: #7796a8;
    padding: 11px 0px;
    border-radius: 4px;
    pointer-events: none;
  }
  .no_default_upsell span a {
    font-size: 1.1875em;
    text-decoration: none;
    color: #fff;
    font-weight: 500;
  }
  .template-product a {
    text-decoration: none !important;
  }
  .swatch input:checked + label {
    background-color: {{ section.settings.swatches_color }};
  }
  .swatch.clearfix {
    width: 100%;
    padding: 4px 0 10px;
    border-top: 1px solid #eee;
  }
  .product-section__form-item.product-section__form-item--quantity label {
    font-size: 0.9375em;
  }
  @media screen and (max-width: 30000px) and (min-width: 768px) {
    .upsell_products form.product-section__form.product-section__form {
    width: 20px;
    }   
  }
  @media screen and (max-width: 30000px) and (min-width: 1024px) {     
    .modal-dialog {
      top:10% !important;
    }
    .upsell_products {
      width: 100% !important;
      float: left !important;
      padding-bottom: 16px;
    }
    .popup_image {
      float: left;
      width: 13%;
    }
    .popup_title {
      float: left ;
      padding-left: 15px ;
    }
    .popup_price {
      width: 35%;
      float: left;
      text-align: left;
      padding-left: 17px;
    }
    .popup_title {
      font-size: 1.125em;
      font-weight: bold;
    }
    button#AddToCart {
      text-transform: capitalize;
      background-color: #ff3b3b;
    }
    .popup_title {
      min-width: 67%;
    }
    .popup_title {
      text-align: left !important;
    }
    .popup_price {
      text-align: left !important;
    }
    .upsell_products  p.ajaxified-cart-feedback.success a {
      display: none;
    }
    .upsell_products  p.ajaxified-cart-feedback.success {
      right: 0px;
    }
  }  
  .modal-footer {
      text-align: center;
  }
  .template-product .modal {
    background: rgba(0, 0, 0, 0.7) !important;  
  }
  .modal-backdrop.fade.in {
    display: none;
  }
  .template-product a.site-header__logo-link {
    font-size: 1.1875em;
    font-weight: bold;    
  }
  .template-product .h2.site-header__logo a {
    font-size: 1.25em;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: bold;
    color: #3D4246;
  }  
  .template-product .h2.site-header__logo {
    padding-top: 0px;
    margin-top: 14.5px;
    margin-bottom: 15px;
  }
</style>
{% schema %}
  {
    "name": "Product Pages",
    "settings": [
      {
        "type": "header",
        "content": "Image Size"
      },
      {
        "type": "select",
        "id": "image_size",
        "label": "Image size",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "medium"
      },
      {
        "type": "header",
        "content": "Product Form"
      },
      {
        "type": "checkbox",
        "id": "show_quantity_selector",
        "label": "Show quantity selector",
        "default": false
      },
      {
        "type": "checkbox",
        "id": "show_variant_labels",
        "label": "Show variant labels",
        "default": true
      },
      {
        "type": "checkbox",
        "id": "show_vendor",
        "label": "Show vendor",
        "default": false
      },
      {
        "type": "checkbox",
        "id": "show_share_buttons",
        "label": "Show social sharing buttons",
        "default": true
      },
      {
        "type": "header",
        "content": "Product Gallery"
      },
      {
        "type": "checkbox",
        "id": "enable_zoom",
        "label": "Enable image zoom",
        "default": true
      },
      {
        "type": "header",
        "content": "Product Name"
      },
      {
        "type": "color",
        "id": "main_product_color",
        "label": "Select Color for product heading"
      },
      {
        "type": "text",
        "id": "main_product_size",
        "label": "Font Size"
      },
      {
        "type": "header",
        "content": "Swatches"
      },
      {
        "type": "checkbox",
        "id": "enable_swatches",
        "label": "Enable Swatches "
      },
      {
        "type": "color",
        "id": "swatches_color",
        "label": "Color Swatches",
        "default": "#000"
      },
      {
        "type": "header",
        "content": "Enable Upsell Products"
      },
      {
        "type": "checkbox",
        "id": "related_product",
        "label": "Enable",
        "default": true
      },
      {
        "type": "header",
        "content": "Product price"
      },
      {
        "type": "color",
        "id": "cut_through_product_color",
        "label": "Select Color for cutthrough price"
      },
      {
        "type": "color",
        "id": "simple_product_color",
        "label": "Select Color for price"
      },
      {
        "type": "text",
        "id": "product_price_text",
        "label": "Font size(Example: 20PX)"
      },
      {
        "type": "header",
        "content": "Product Description"
      },
      {
        "type": "text",
        "id": "product_description_text",
        "label": "Font size"
      },
      {
        "type": "header",
        "content": "Button Settings"
      },
      {
        "type": "text",
        "id": "font_size",
        "label": "Enter font size for the Add to Cart"
      },
      {
        "type": "text",
        "id": "stock_value_1",
        "label": "Enter text before stock value",
        "default": "Hurry only"
      },
      {
        "type": "text",
        "id": "stock_value_2",
        "label": "Enter text after stock value",
        "default": "left in stock!"
      },
      {
        "type": "header",
        "content": "Instagram Gallery"
      },
      {
        "type": "checkbox",
        "id": "show_empty_gallery_text",
        "label": "Enable empty instagram gallery message",
        "default": true
      },
      {
        "type": "header",
        "content": "Fixed Position of Add To Cart Button"
      },
      {
        "type": "checkbox",
        "id": "Fixed",
        "label": "Make fixed",
        "default": false
      },
      {
        "type": "header",
        "content": "Social sharing"
      },
      {
        "type": "checkbox",
        "id": "top_enable",
        "label": "Enable Social sharing for top"
      }
    ]
  }
{% endschema %}